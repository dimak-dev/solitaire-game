/**
 * There are two mode of game.
 *
 * @property ONE Player pick only one card from stock.
 * @property THREE Player pick three cards from stock and can interact only with last one from waste..
 */
export enum EPickingFromStockCount {
    ONE,
    THREE,
}